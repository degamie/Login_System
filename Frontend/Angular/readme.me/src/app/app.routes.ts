import { Routes } from '@angular/router';
import { AuthGuardLoginComponent } from '../auth-guard-login/auth-guard-login.component';

export const routes: Routes = [
    imports[
        providers[
            AuthGuardLoginComponent;
        ]
    ]

];